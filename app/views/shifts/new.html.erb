<div class="main">
  <h1><%=params[:date].to_time.strftime('%Y年%-m月%-d日')%></h1>
  <p>希望する業務時間を選択してください。</p>
  <div class="row">
    <div class="shift-form">
      <div class="col-md-6 col-md-offset-3">
        <%= form_with(model: @shift , local: true,class:"form-inline") do |f| %>
        <%= render 'shared/shift_error_messages' %>

        <%= f.label :始業時刻 %>
        <%= f.time_field(:begin_time, class: 'form form-control', value:Time.now.strftime( "%H:%M" ),prompt:true, ignore_data:true) %>


        <%= f.label :終業時刻%>
        <%= f.time_field(:finish_time, class: 'form form-control', value:Time.now.strftime( "%H:%M" )) %>

        <%=f.hidden_field(:user_id, value: params[:user_id])%>

        <%=f.hidden_field(:date, value: params[:date])%>

        <%=f.hidden_field(:start_time ,value: params[:date])%>

        <%= f.submit "登録", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>